#!/usr/bin/env python

# This is a shim to hopefully allow Github to detect the package, build is done
# with poetry

from setuptools import setup

if __name__ == "__main__":
    setup(name="CoRAL", version="2.0.0")
